# Design System Components

Reusable accessible primitives in this directory. They are intentionally domain-neutral and use
React Aria Components as the single interaction foundation. Phase 1 added card variants
(`elevated` / `flush` / `subtle` / `interactive`) and refined the button, input, menu, tab, and
typography primitives (see the Phase 1 section in `ARTIFACTS.md`).
