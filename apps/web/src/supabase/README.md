# Supabase Browser Boundary

`browser.ts` is the only browser-safe Supabase adapter. It accepts only the public URL and
publishable key. It is not imported by the current route shell and does not implement authentication
workflows.
