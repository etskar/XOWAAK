/**
 * This is an explicit pre-generation placeholder, not generated schema output.
 * Run `pnpm supabase:types` after a local or remote Supabase schema is available.
 */
export type Database = never;
